# AGENTS.md — دليفري طرطوس

## 1. هوية المشروع

**اسم المشروع:** دليفري طرطوس (Delivery Tartous).

المشروع تطبيق موبايل داخلي لإدارة وتوصيل الطلبات. يوجد تطبيق واحد مبني بـExpo وReact Native وTypeScript، لكن تظهر لكل مستخدم واجهة ووظائف مختلفة وفق دوره. التطبيق يجب أن يكون عربيًا بالكامل ويدعم اتجاه الكتابة من اليمين إلى اليسار (RTL).

> لا توجد واجهات نهائية من اجتهاد الوكيل. **Google Stitch هو المصدر البصري المعتمد للتصاميم**. قبل وجود تصميم معتمد، يُسمح فقط ببناء البنية، الأنواع، المنطق والمكونات المحايدة.

---

## 2. الأدوار والصلاحيات الأساسية

| الدور | الهدف التشغيلي | الصلاحيات الأساسية |
| --- | --- | --- |
| `admin` | إدارة النظام والتشغيل والمال | إدارة الأدمنز والمشرفين والكباتن، جميع الطلبات، جميع الأرباح، السجل، وتعديل صلاحيات المشرفين. |
| `supervisor` | إدارة التشغيل اليومي | إنشاء وإلغاء الطلبات، إدارة الكباتن فقط، الأمانات، كشف الأجور وتسجيل دفعات الكباتن. لا يغير Admin أو Supervisor أو الأدوار أو Permission Overrides. |
| `captain` | تنفيذ التوصيل | تغيير حالته إلى متاح/غير متاح، استقبال الطلب المعيّن له، تحديث حالة الطلب، رؤية طلباته ومستحقاته فقط. |

كل أدمن يملك حالياً صلاحيات كاملة متساوية. الكابتن لا يستطيع إنشاء طلب ولا تعديل طلب غير معيّن له.

---

## 3. دورة حياة الطلب

استخدم هذه الحالات كمرجع واحد داخل التطبيق وقاعدة البيانات لاحقاً:

```
pending       = طلب جديد
assigned      = عُيّن لكابتن محدد
received      = الكابتن أكد الاستلام
in_delivery   = قيد التوصيل
completed     = تم التوصيل واستلام الأجرة
cancelled     = أُلغي من الأدمن أو المشرف مع سبب
false_order   = طلب كاذب سجله الكابتن
```

التسلسل الطبيعي:

```
pending → assigned → received → in_delivery → completed
```

الحالتان `cancelled` و`false_order` نهائيتان ولا يعود الطلب منهما للحالات التشغيلية إلا بقرار صريح ومنطق جديد معتمد.

---

## 4. قواعد الأرباح

| نوع الطلب | مستحق الكابتن | ربح الشركة | ملاحظة |
| --- | --- | --- | --- |
| `completed` | 70% من الأجرة | 30% من الأجرة | النسبة 30% لصالح الشركة. |
| `false_order` | 70% من الأجرة | 0% | الـ30% لا تُسجل كربح للشركة، بل كبند خصم/تسوية منفصل قابل للمراجعة. |
| `cancelled` | 0 | 0 | لا يدخل في الأرباح. |

لا يسمح لتطبيق الموبايل بتعديل مبالغ الأرباح أو تحويل حالة مالية حساسة مباشرة. لاحقاً تنفذ هذه الحسابات داخل Supabase RPC/Database Functions.

---

## 5. البنية التقنية المعتمدة

| الطبقة | التقنية |
| --- | --- |
| الموبايل | Expo + React Native + TypeScript + Expo Router |
| الواجهات | تصاميم Google Stitch المعتمدة ثم تحويلها إلى React Native |
| الباك-إند لاحقاً | Supabase Auth + PostgreSQL + RLS + RPC + Edge Functions |
| المزامنة | Supabase Realtime لاحقاً |
| إشعارات Android | Expo Notifications لاحقاً |
| التحكم بالإصدارات | GitHub Private repository للنسخة الجديدة فقط |

لا يتم استخدام Express أو Firebase أو أي API خارجي في مرحلة الواجهات والبنية الأولية ما لم يعتمد صاحب المشروع ذلك صراحة.

---

## 6. المصادقة والأمان المستقبلي

لاحقاً فقط، بعد اعتماد الواجهات، سيكون الدخول عبر Supabase Auth.

```
تسجيل الدخول → Supabase Session موقّعة → user_id → قراءة role → التوجيه للواجهة المناسبة
```

القواعد غير القابلة للنقاش:

1. لا تُخزّن `SERVICE_ROLE_KEY` أو JWT signing secret أو كلمات مرور في تطبيق Expo أو Git.

1. لا تعتمد على إخفاء الأزرار كحماية؛ لاحقاً RLS هي طبقة الحماية الأساسية للبيانات.

1. لا يستطيع الكابتن رؤية أو تعديل إلا الطلبات المعيّنة له.

1. العمليات الحساسة مثل قبول الطلب، إكمال الطلب، الحسابات، الدعوات والإشعارات تنفذ في RPC أو Edge Functions بعد مرحلة Supabase.

1. لا تنشئ Supabase ولا مفاتيح بيئة ولا Auth قبل أمر واضح من صاحب المشروع.

---

## 6-A. عقد Supabase النشط — أولوية إلزامية

> **هذه الفقرة تعلو على أي نص أقدم في هذا الملف يقول إن Supabase مرحلة لاحقة.** مشروع Supabase وRLS وRPCs وEdge Function أصبحت موجودة فعلياً ومتصلة بالمشروع.

1. اقرأ `SUPABASE_BACKEND_CONTRACT.md` قبل أي تعديل له علاقة بتسجيل الدخول أو المستخدمين أو الطلبات أو الأرباح أو الأمانات.

2. استخدم فقط `src/data/supabase/supabaseContract.ts` من الشاشات والـHooks. لا تستورد `getSupabaseClient()` خارج `src/data/supabase/`.

3. ملف `src/data/supabase/database.types.ts` مولّد من قاعدة البيانات الحية. **ممنوع تعديله يدوياً أو إنشاء Types يدوية موازية للجداول وRPCs.**

4. جميع تعديلات الطلبات والأرباح والدفعات والمستخدمين والأمانات تحدث عبر RPC/Edge Function المعرفة في العقد؛ ممنوع `insert` أو `update` أو `delete` مباشر من تطبيق Expo على هذه الجداول.

5. ممنوع `auth.signUp()` داخل تطبيق Expo وممنوع استدعاء `invite-user` المتقاعد. الأدمن أو المشرف ينشئ Pending عبر `deliverySupabase.actions.createPendingAccount`، ثم يفعّل المستخدم الحساب داخل التطبيق عبر `deliverySupabase.auth.activatePendingAccount` باستخدام البريد وكلمة المرور وتأكيدها.

6. لا تغيّر Supabase schema أو RLS أو RPC أو Edge Function ولا تطبق Migration إلا بطلب صريح من صاحب المشروع. بعد أي Migration معتمدة: يُعاد توليد `database.types.ts` ثم يحدّث العقد.

---

## 7. قواعد العمل الإلزامية للوكيل

### Workspace وGit

1. اعمل دائماً في **Local Workspace** الحالي، ولا تنشئ `Worktree` أو جلسة سحابية منفصلة.

1. لا تستخدم `git commit` أو `git push` أو `git reset` أو `git clean` إلا بطلب صريح من صاحب المشروع.

1. لا تحذف ملفات أو مجلدات موجودة إلا بعد ذكر القائمة وانتظار موافقة صريحة.

1. لا تثبّت dependency جديدة ولا تعدّل `package.json` دون بيان السبب والحصول على موافقة صريحة.

1. بعد كل مهمة، اذكر الملفات التي عدلتها فقط ونتيجة فحص TypeScript إن طُلب.

اقرأ AGENTS.md أولاً، ثم أضف في آخره عنواناً دائماً:

## Functional Completion Gate — Mandatory

لا تعتبر أي ميزة، شاشة، زر، أو RPC منتهياً لمجرد أن TypeScript ينجح.

قبل كتابة الكود، عرّف لكل عملية:
1. Inputs: الحقول المطلوبة، نوعها، حدّها، والقيم غير المسموحة.
2. Preconditions: الدور المسموح، حالة السجل المطلوبة، ومنع التكرار.
3. Backend action: RPC أو method المسموح حصراً من deliverySupabase.
4. Success postconditions: ماذا يتغير بالبيانات وماذا يجب أن يراه المستخدم فوراً بعد النجاح.
5. Failure postconditions: ماذا يبقى كما هو، رسالة الخطأ الظاهرة، وإعادة كل loading state إلى idle.
6. Repeat/concurrency: ماذا يحصل عند الضغط مرتين أو إعادة العملية على سجل تغيرت حالته.

لكل Mutation في React Native:
- تحقق من المدخلات محلياً قبل setLoading وقبل أي طلب شبكة.
- استعمل حالة مستقلة لكل عملية وكل سجل عند اللزوم، مثل creating وcancellingId؛ ممنوع loading عام يجمّد الشاشة.
- بعد النجاح حدّث state المحلي بشكل محدد، أو أعد الجلب عند الحاجة، ثم اعرض الحالة الجديدة الصحيحة فقط.
- لا تعرض pending record بعد cancelled_at أو activated_at على أنه قابل للإلغاء.
- اعرض رسالة الخطأ الفعلية؛ ممنوع catch يخفي السبب.
- finally يعيد الحالة إلى idle دائماً.
- أي طلب شبكة له مهلة واضحة؛ ممنوع spinner بلا سقف زمني.
- امنع التكرار عبر disabled/state lock، ولا تعتمد على إخفاء الزر كحماية وحيدة.

قبل تسليم


### الواجهات

1. لا تخترع شكل Dashboard أو شاشة نهائية من عندك؛ التصميم يأتي من Google Stitch أو صورة مرجعية معتمدة.

1. التزم بالعربية وRTL في كل نص واجهة.

1. الألوان الأساسية للهوية:

```
primary: '#0060B8'
primaryDark: '#003878'
primaryLight: '#0080D8'
primarySoft: '#EAF4FC'
background: '#F7F9FC'
surface: '#FFFFFF'
text: '#14213D'
muted: '#64748B'
success: '#16A34A'
warning: '#F59E0B'
error: '#DC2626'
```

1. لا تستخدم صور أشخاص حقيقية كـavatars؛ استخدم placeholders أو أيقونات عند الحاجة.

1. لا تضف خريطة أو دفع أو صور إثبات أو تتبع حي إلا بعد اعتمادها صراحة.

### التنفيذ

1. اقرأ هذا الملف قبل البدء بأي مهمة.

1. افحص بنية المشروع الحالية قبل تعديل مسارات Expo Router.

1. نفذ المهمة المطلوبة فقط؛ لا تبدأ شاشة أو ميزة تالية من تلقاء نفسك.

1. لا تخفِ أخطاء lint أو TypeScript عبر overrides أو `--force` أو `--legacy-peer-deps` من دون شرح وموافقة.

1. عند وجود غموض مؤثر، اسأل بدلاً من الافتراض.

---

## 8. الترتيب الحالي للتنفيذ

```
1. إنشاء مشروع Expo جديد ومجلد نظيف.
2. إنشاء Repository خاص جديد للنسخة الجديدة وربطه بالمشروع.
3. وضع هذا الملف باسم AGENTS.md في جذر المشروع.
4. تصميم واعتماد شاشات الأدمن والمشرف والكابتن في Google Stitch.
5. إدخال التصاميم المعتمدة إلى المشروع.
6. بناء مكونات مشتركة ومنطق محلي تحت التصاميم.
7. اختبار الواجهات على Expo Go.
8. بدء Supabase وAuth وRLS وRPC لاحقاً.
```

---

## 9. تعريف النجاح للمرحلة الحالية

المرحلة الحالية ليست بناء نظام طلبات أو قواعد بيانات. النجاح الآن يعني:

- وجود مشروع Expo نظيف في Local Workspace.

- وجود هذا الملف في جذر المشروع باسم `AGENTS.md`.

- اعتماد تصميم Stitch واحد على الأقل لكل دور قبل بناء واجهاته النهائية.

- عدم وجود Worktree منفصل أو كود مفقود خارج مشروع المستخدم المحلي.

- عدم تعديل أي أسرار أو إعدادات باك-إند.

> لا تبدأ بالبرمجة من تلقاء نفسك بعد قراءة الملف. انتظر المهمة التالية المحددة من صاحب المشروع.