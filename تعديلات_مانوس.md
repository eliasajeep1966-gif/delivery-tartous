# تعديلات مانوس — Realtime الكابتن

```ts
/**
 * الهدف:
 * عند إسناد طلب جديد إلى الكابتن، يظهر الطلب تلقائياً في تطبيقه
 * ويظهر له إشعار واضح. وعند تغيير حالة الطلب، تعيد شاشة الكابتن
 * تحميل بياناتها فوراً من العقد الخلفي الموثوق.
 */
```

| الملف | التعديل | النتيجة |
|---|---|---|
| `supabase/migrations/20260822211000_enable_orders_realtime.sql` | إضافة جدول `orders` إلى Publication اسمها `supabase_realtime`. | Supabase يرسل أحداث إنشاء وتحديث الطلبات إلى المشتركين المسموح لهم. |
| `web/client/src/data/supabase/webSupabaseContract.ts` | إضافة `subscribeToCaptainOrders(captainId, onChange)`. | اشتراك واحد فقط، مفلتر بـ`assigned_captain_id`، ويغلق تلقائياً عند الخروج. |
| `web/client/src/features/captain/useCaptainDashboard.ts` | تشغيل الاشتراك، دمج الأحداث المتقاربة خلال 250ms، ثم إعادة تحميل بيانات الكابتن من RPCs الحالية. | الطلب المعيّن يظهر دون Refresh، وتبقى RLS هي مصدر التفويض الحقيقي. |
| `web/client/src/features/captain/useCaptainDashboard.ts` | مقارنة الطلبات قبل وبعد إعادة التحميل. | عند ظهور طلب جديد بحالة `assigned` فقط، يتم تسجيله كإشعار جديد. لا يظهر إشعار عند فتح التطبيق إذا كان الطلب موجوداً مسبقاً. |
| `web/client/src/pages/CaptainHome.tsx` | إضافة Toast وبطاقة تنبيه ثابتة «وصلك طلب جديد». | الكابتن يرى إشعاراً فورياً مع رقم الطلب واسم العميل، ويمكنه إخفاء البطاقة بزر «تم». |

```ts
/**
 * تسلسل العمل عند إسناد طلب:
 *
 * admin/supervisor يعيّن الطلب للكابتن
 *   -> حدث INSERT أو UPDATE يصل عبر Supabase Realtime
 *   -> Hook الكابتن يجمع الحدث لمدة 250ms
 *   -> يعيد قراءة captainOrders + captainHomeMetrics + orderStops
 *   -> تظهر بطاقة الطلب الحالي
 *   -> يظهر Toast وإشعار "وصلك طلب جديد"
 */
```

```ts
/**
 * تسلسل العمل عند تغيير حالة الطلب:
 *
 * الكابتن يضغط: استلام الطلب / بدء التوصيل / تأكيد التسليم
 *   -> RPC transitionAssignedOrder تطبق التغيير في الخلفية
 *   -> Event UPDATE يصل للاشتراك
 *   -> إعادة تحميل بيانات الكابتن تلقائياً
 *   -> تتحدث بطاقة الطلب، الخط الزمني، والملخص اليومي
 *
 * أي تعديل من الإدارة على طلب الكابتن يمر بنفس التسلسل.
 */
```

## الحماية والاستهلاك

```ts
/**
 * لا توجد قناة عامة لكل الطلبات.
 * الاشتراك مفصول للكابتن الحالي فقط عبر assigned_captain_id.
 * لا تُعرض بيانات event الخام مباشرة في الواجهة.
 * بعد كل event، التطبيق يعيد القراءة من RPCs وRLS الحالية.
 * عند خروج الكابتن من الشاشة أو تغيّر جلسة الدخول، تُغلق القناة.
 */
```

## ما لم يُنفذ في هذه المرحلة

```ts
/**
 * لا يوجد Push Notification لنظام الهاتف عند إغلاق التطبيق.
 * الإشعار الحالي In-App: يعمل عندما تكون واجهة الكابتن مفتوحة.
 * لا توجد Realtime للأجور أو الأمانات أو سجل الحركات حالياً.
 * لا توجد Realtime للوحة الأدمن/المشرف بعد؛ المرحلة الحالية للكابتن فقط.
 */
```

## الفحوص المنفذة

```text
pnpm --dir web check  ✅
pnpm --dir web build  ✅
git diff --check      ✅
```

> ملاحظة: ظهر تحذير Vite المعتاد عن حجم JavaScript chunk فقط، وليس خطأ وظيفياً.
