/** Design reminder — Corporate Modern Mobile Operations: RTL captain operations, #0060B8 hierarchy, white cards, Cairo typography. */
import { useMemo, useState } from "react";
import { CheckCheck, CircleOff, PackageCheck, Search, ShieldCheck, Truck, UserRound } from "lucide-react";
import { toast } from "sonner";
import { AdminBottomNav } from "@/components/AdminBottomNav";
import { Drawer, DrawerContent, DrawerDescription, DrawerHeader, DrawerTitle } from "@/components/ui/drawer";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { captains, orderDetails } from "@/mocks/dashboard-data";
import type { AdminCaptainListItem } from "@/features/admin/types";

type CaptainFilter = "all" | "available" | "unavailable" | "active" | "inactive";
type CaptainsProps = {
  params?: Record<number, string | undefined>;
  onSetCaptainActive?: (captainId: string, active: boolean) => void;
  onManageCustody?: (captainId: string, action: "assign" | "return") => void;
};

const filters: { id: CaptainFilter; label: string }[] = [{ id: "all", label: "الكل" }, { id: "available", label: "متاح" }, { id: "unavailable", label: "غير متاح" }, { id: "active", label: "مفعل" }, { id: "inactive", label: "معطل" }];

function CaptainDetailsDrawer({ captain, open, onOpenChange, onSetCaptainActive, onManageCustody }: { captain: AdminCaptainListItem | null; open: boolean; onOpenChange: (open: boolean) => void; onSetCaptainActive?: CaptainsProps["onSetCaptainActive"]; onManageCustody?: CaptainsProps["onManageCustody"] }) {
  if (!captain) return null;
  const captainOrders = orderDetails.filter((order) => order.captain?.id === captain.id);
  const requestActivation = () => onSetCaptainActive?.(captain.id, captain.activation !== "active") ?? toast.info(`${captain.activation === "active" ? "تعطيل" : "تفعيل"} الكابتن جاهز للربط مع set_captain_active.`);
  const requestCustody = (action: "assign" | "return") => onManageCustody?.(captain.id, action) ?? toast.info(`${action === "assign" ? "إضافة" : "إرجاع"} الأمانة جاهز للربط مع إجراءات الأمانات.`);

  return <Drawer open={open} onOpenChange={onOpenChange} direction="right"><DrawerContent className="h-full w-[92%] max-w-[430px] overflow-y-auto border-l-[#cfe1f0] bg-[#f0f7ff]" dir="rtl"><DrawerHeader className="border-b border-[#dbe7f2] bg-white text-right"><DrawerTitle className="text-right text-[19px] text-[#1c1b1b]">تفاصيل الكابتن</DrawerTitle><DrawerDescription className="text-right text-xs text-[#58616b]">بيانات عرض تجريبية جاهزة لمصدر البيانات لاحقاً.</DrawerDescription></DrawerHeader><div className="p-4"><section className="rounded-2xl border border-[#dbe7f2] bg-white p-4 shadow-[0_2px_8px_rgba(0,72,141,0.05)]"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2.5"><span className="grid h-12 w-12 place-items-center rounded-full bg-[#e7edf2] text-lg font-bold text-[#52606d]">{captain.initial}</span><div><h2 className="text-[17px] font-bold">{captain.name}</h2><div className="mt-1 flex flex-wrap gap-1.5"><span className={`rounded-md px-2 py-0.5 text-[10px] font-bold ${captain.availability === "available" ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-700"}`}>{captain.availability === "available" ? "متاح" : "غير متاح"}</span><span className={`rounded-md px-2 py-0.5 text-[10px] font-bold ${captain.activation === "active" ? "bg-blue-50 text-[#0060B8]" : "bg-red-50 text-[#ba1a1a]"}`}>{captain.activation === "active" ? "مفعل" : "معطل"}</span></div></div></div><Truck className="text-[#0060B8]" size={22} /></div><button type="button" onClick={requestActivation} className={`mt-4 flex h-10 w-full items-center justify-center gap-2 rounded-xl text-xs font-bold active:scale-[0.98] ${captain.activation === "active" ? "border border-red-200 bg-red-50 text-[#ba1a1a]" : "border border-emerald-200 bg-emerald-50 text-emerald-700"}`}>{captain.activation === "active" ? <CircleOff size={16} /> : <ShieldCheck size={16} />}{captain.activation === "active" ? "تعطيل الكابتن" : "تفعيل الكابتن"}</button></section><Tabs defaultValue="summary" className="mt-4"><TabsList className="w-full bg-white p-1 shadow-[0_1px_4px_rgba(0,72,141,0.05)]"><TabsTrigger value="summary" className="text-xs">ملخص</TabsTrigger><TabsTrigger value="orders" className="text-xs">الطلبات</TabsTrigger><TabsTrigger value="custody" className="text-xs">الأمانات</TabsTrigger></TabsList><TabsContent value="summary" className="mt-3"><section className="grid grid-cols-2 gap-3"><article className="rounded-2xl border border-[#dbe7f2] bg-white p-3.5"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#eaf4ff] text-[#0060B8]"><CheckCheck size={18} /></span><p className="mt-3 text-[11px] text-[#66727e]">طلبات مكتملة</p><strong className="mt-1 block text-lg">{captain.completedOrders}</strong></article><article className="rounded-2xl border border-[#dbe7f2] bg-white p-3.5"><span className="grid h-9 w-9 place-items-center rounded-xl bg-amber-100 text-amber-700"><PackageCheck size={18} /></span><p className="mt-3 text-[11px] text-[#66727e]">الأمانات الحالية</p><strong className="mt-1 block text-lg">{captain.custodyItems.filter((item) => item.status === "held").length}</strong></article></section>{captain.currentOrderId && <div className="mt-3 rounded-2xl border border-[#dbe7f2] bg-white p-3.5 text-xs text-[#58616b]">الطلب الحالي: <strong className="text-[#0060B8]">#{captain.currentOrderId}</strong></div>}</TabsContent><TabsContent value="orders" className="mt-3"><section className="space-y-2">{captainOrders.length ? captainOrders.map((order) => <article key={order.id} className="rounded-2xl border border-[#dbe7f2] bg-white p-3.5"><div className="flex items-center justify-between"><strong className="text-sm">#{order.id}</strong><span className="text-[10px] text-[#66727e]">{order.timestamp}</span></div><p className="mt-1 text-xs">{order.customer} — {order.location}</p></article>) : <div className="rounded-2xl border border-dashed border-[#c7dae8] bg-white/70 px-4 py-8 text-center text-xs text-[#75818e]">لا توجد طلبات عرض مرتبطة بهذا الكابتن.</div>}</section></TabsContent><TabsContent value="custody" className="mt-3"><section className="rounded-2xl border border-[#ecd6a5] bg-white p-3.5"><div className="space-y-2">{captain.custodyItems.map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl bg-[#f5f9fc] px-3 py-2"><span className="text-xs font-bold">{item.label}</span><span className={`text-[10px] font-bold ${item.status === "held" ? "text-amber-700" : "text-emerald-700"}`}>{item.status === "held" ? "مع الكابتن" : "تم الإرجاع"}</span></div>)}</div><div className="mt-3 grid grid-cols-2 gap-2"><button type="button" onClick={() => requestCustody("assign")} className="h-10 rounded-xl border border-amber-200 bg-amber-50 text-xs font-bold text-amber-700 active:scale-[0.98]">إضافة أمانة</button><button type="button" onClick={() => requestCustody("return")} className="h-10 rounded-xl border border-emerald-200 bg-emerald-50 text-xs font-bold text-emerald-700 active:scale-[0.98]">إرجاع أمانة</button></div></section></TabsContent></Tabs></div></DrawerContent></Drawer>;
}

export default function Captains({ onSetCaptainActive, onManageCustody }: CaptainsProps) {
  const [filter, setFilter] = useState<CaptainFilter>("all");
  const [query, setQuery] = useState("");
  const [selectedCaptain, setSelectedCaptain] = useState<AdminCaptainListItem | null>(null);
  const visibleCaptains = useMemo(() => { const normalized = query.trim().toLowerCase(); return captains.filter((captain) => (filter === "all" || captain.availability === filter || captain.activation === filter) && (!normalized || captain.name.toLowerCase().includes(normalized))); }, [filter, query]);

  return <div className="min-h-screen bg-[#eaf5ff] text-[#1c1b1b]" dir="rtl"><div className="relative mx-auto min-h-screen w-full max-w-[453px] overflow-x-hidden bg-[#f0f7ff] shadow-[0_0_40px_rgba(0,72,141,0.08)]"><header className="fixed top-0 right-0 left-0 z-30 mx-auto flex h-16 w-full max-w-[453px] items-center justify-between bg-[#0060B8] px-5 text-white shadow-[0_4px_18px_rgba(0,96,184,0.28)]"><span className="grid h-10 w-10 place-items-center rounded-full bg-white/10"><Truck size={21} /></span><div className="flex items-center gap-2"><div className="text-left"><p className="text-[11px] leading-4 text-[#dbeaff]">لوحة الأدمن</p><h1 className="text-[19px] font-bold leading-6">الكباتن</h1></div><span className="grid h-9 w-9 place-items-center rounded-xl bg-white/15"><UserRound size={21} /></span></div></header><main className="px-5 pt-[84px] pb-24"><section className="rounded-2xl border border-[#d3e3f0] bg-white p-4 shadow-[0_2px_8px_rgba(0,72,141,0.05)]"><div className="flex items-start justify-between gap-3"><div><h2 className="text-[18px] font-bold">إدارة الكباتن</h2><p className="mt-1 text-xs leading-5 text-[#58616b]">تابع التوفر والتفعيل والأمانات والطلبات.</p></div><span className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-[#eaf4ff] text-[#0060B8]"><Truck size={23} /></span></div><div className="relative mt-4"><Search className="pointer-events-none absolute top-1/2 right-3 -translate-y-1/2 text-[#75818e]" size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="ابحث باسم الكابتن" className="h-11 w-full rounded-xl border border-[#c9d9e7] bg-[#fbfdff] pr-10 pl-3 text-sm placeholder:text-[#8a98a6] focus:border-[#0060B8] focus:outline-none focus:ring-2 focus:ring-[#0060B8]/15" /></div></section><section className="mt-4 flex gap-2 overflow-x-auto pb-1 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">{filters.map((item) => <button type="button" key={item.id} onClick={() => setFilter(item.id)} className={`h-8 shrink-0 rounded-full px-3.5 text-xs font-bold active:scale-[0.96] ${filter === item.id ? "bg-[#0060B8] text-white" : "border border-[#d4e2ec] bg-white text-[#58616b]"}`}>{item.label}</button>)}</section><section className="mt-5"><div className="mb-3 flex items-center justify-between"><h2 className="text-base font-bold">الكباتن المعروضون</h2><span className="rounded-full bg-[#dbeeff] px-2.5 py-1 text-xs font-bold text-[#0060B8]">{visibleCaptains.length} كباتن</span></div><div className="space-y-3">{visibleCaptains.length ? visibleCaptains.map((captain) => <button type="button" key={captain.id} onClick={() => setSelectedCaptain(captain)} className="w-full rounded-2xl border border-[#dbe7f2] bg-white p-4 text-right shadow-[0_2px_8px_rgba(0,72,141,0.05)] transition-all hover:-translate-y-px hover:shadow-[0_5px_12px_rgba(0,72,141,0.08)] active:scale-[0.99]"><span className="flex items-start justify-between gap-3"><span className="flex items-center gap-2.5"><span className="grid h-11 w-11 place-items-center rounded-full bg-[#e7edf2] text-base font-bold text-[#52606d]">{captain.initial}</span><span><strong className="block text-[15px]">{captain.name}</strong><span className="mt-1 flex gap-1.5"><span className={`rounded-md px-2 py-0.5 text-[10px] font-bold ${captain.availability === "available" ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-700"}`}>{captain.availability === "available" ? "متاح" : "غير متاح"}</span><span className={`rounded-md px-2 py-0.5 text-[10px] font-bold ${captain.activation === "active" ? "bg-blue-50 text-[#0060B8]" : "bg-red-50 text-[#ba1a1a]"}`}>{captain.activation === "active" ? "مفعل" : "معطل"}</span></span></span></span><span className="text-left text-[11px] text-[#66727e]"><strong className="block text-sm text-[#1c1b1b]">{captain.completedOrders}</strong>مكتمل</span></span><span className="mt-3 flex items-center justify-between border-t border-[#eef3f7] pt-3 text-[11px] text-[#66727e]"><span>{captain.currentOrderId ? `الطلب الحالي #${captain.currentOrderId}` : "لا يوجد طلب حالي"}</span><span>{captain.custodyItems.filter((item) => item.status === "held").length} أمانات</span></span></button>) : <div className="rounded-2xl border border-dashed border-[#c7dae8] bg-white/70 px-4 py-10 text-center text-sm text-[#75818e]">لا توجد كباتن مطابقة للفلتر.</div>}</div></section></main><AdminBottomNav active="captains" /><CaptainDetailsDrawer captain={selectedCaptain} open={selectedCaptain !== null} onOpenChange={(open) => !open && setSelectedCaptain(null)} onSetCaptainActive={onSetCaptainActive} onManageCustody={onManageCustody} /></div></div>;
}
