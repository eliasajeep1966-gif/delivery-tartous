## 10. معيار التنفيذ الوظيفي للواجهات — دائم

> هذا القسم يكمّل التعليمات السابقة ولا يلغي عقد Supabase أو قواعد Git أو مصدر التصميم المعتمد. طبّقه على كل شاشة أو زر أو Hook يقرأ أو يغيّر بيانات التطبيق.

### 10.1 المبدأ

لا تسلّم كوداً يترجم فقط. اعتبر كل ميزة منتهية فقط عندما تكون دورة **المدخلات → التحقق → العملية → النتيجة → حالة الواجهة** واضحة ومغطاة.

لا تبنِ منطقاً تجميلياً منفصلاً عن المخرجات الحقيقية. بعد كل Mutation يجب أن يرى المستخدم النتيجة الصحيحة فوراً: سجل ظهر، سجل اختفى، حالة تغيّرت، أو خطأ مفهوم ظهر.

### 10.2 قبل كتابة أي عملية

عرّف داخل تعليق مختصر أو خطة المهمة العناصر التالية قبل التنفيذ:

| العنصر | المطلوب |
| --- | --- |
| Inputs | الحقول المطلوبة، النوع، الحدود، والقيم غير المقبولة. |
| Permission | الدور أو الصلاحية المطلوبة. لا تعتمد على الواجهة كطبقة حماية. |
| Precondition | حالة السجل التي تسمح بالعملية، مثل Pending فقط للإلغاء أو Assigned فقط للاستلام. |
| Action | الدالة الوحيدة من `deliverySupabase` أو الـRPC/Edge Function المعتمدة في العقد. |
| Success | ما الذي يتغيّر في قاعدة البيانات وما الذي يتغيّر في state والواجهة مباشرة. |
| Failure | ما الذي يبقى كما هو، وما الرسالة المفهومة التي تظهر، وكيف تعود الواجهة إلى idle. |
| Repeat | أثر ضغط الزر مرتين، أو إعادة العملية بعد نجاحها، أو وصول رد متأخر. |

إذا كان أي عنصر غامضاً، لا تخمّن API أو حالة طلب أو صلاحية. اقرأ `SUPABASE_BACKEND_CONTRACT.md` ثم اسأل عن الغموض المؤثر.

### 10.3 قواعد Mutation في React Native

1. تحقّق من المدخلات محلياً **قبل** تغيير أي loading state أو إرسال طلب شبكة. استخدم قيمة مطبّعة عند الحاجة، مثل `email.trim().toLowerCase()`.

1. استخدم حالات مستقلة وواضحة. أمثلة: `isCreating`, `cancellingId`, `transitioningOrderId`, `loadingList`. ممنوع `loading` عام يوقف كامل الشاشة بسبب عملية على سجل واحد.

1. عطّل الزر المرتبط بالعملية نفسها فقط أثناء تنفيذها، وغيّر نصه ليصف العملية مثل «جارٍ الحفظ...» أو «جارٍ الإلغاء...». لا تعطّل أزرار سجلات أخرى بلا سبب.

1. بعد النجاح، حدّث state المحلي بطريقة محددة من نتيجة الخادم، أو أعد جلب مصدر الحقيقة عندما تكون النتيجة مركبة. لا تترك الواجهة تعرض الحالة السابقة.

1. إذا أصبح السجل غير صالح للعملية بعد نجاحها، أزله من القائمة التشغيلية أو اعرض حالته الصحيحة. مثال: لا يظهر Pending ملغى أو مفعّل كأنه قابل للإلغاء مرة ثانية.

1. استخدم `try / catch / finally` لكل Mutation. يجب أن يعيد `finally` حالة العملية إلى idle دائماً، حتى عند خطأ شبكة أو خطأ RPC أو خروج المستخدم من الشاشة.

1. لا تستخدم `catch { رسالة عامة }` يخفي التشخيص. استخرج رسالة آمنة للمستخدم من `Error.message`، وسجّل السبب التقني في `console.error` أثناء التطوير. لا تعرض stack trace أو أسرار أو تفاصيل SQL للمستخدم.

1. ضع مهلة مرئية معقولة لطلبات الشبكة الطويلة. عند انتهاء المهلة، أعد الحالة إلى idle وأظهر رسالة اتصال واضحة. لا تعِد Mutation تلقائياً بعد timeout؛ قد يكون الخادم أنجزها. أعد قراءة القائمة أو الحالة من الخادم قبل السماح بإعادة العملية.

1. امنع التكرار محلياً أثناء العملية، واعتمد أيضاً على RPC/RLS/قيود قاعدة البيانات لمنع التكرار نهائياً. تعامل مع خطأ duplicate أو السجل غير المناسب كحالة مستخدم مفهومة، لا كتعطل صامت.

1. لا تعدّل جداول الحماية مباشرة من الشاشة. استخدم `deliverySupabase` فقط كما يحدد العقد.

### 10.4 حالات الواجهة الإلزامية

كل شاشة بيانات أو قائمة يجب أن تملك حالات منفصلة وواضحة:

| الحالة | السلوك المطلوب |
| --- | --- |
| Initial loading | مؤشر تحميل واضح؛ لا تعرض قائمة فارغة على أنها نتيجة نهائية. |
| Content | بيانات صحيحة مع مفاتيح ثابتة لكل عنصر. |
| Empty | رسالة مرتبطة بالسياق وزر إجراء مناسب عند وجوده. |
| Read error | رسالة مفهومة مع زر إعادة محاولة للقراءة فقط. |
| Mutation pending | حالة على الزر أو السجل المعني فقط. |
| Mutation error | تظهر رسالة الخطأ، وتبقى البيانات السابقة سليمة، ويصبح الإجراء قابلاً للمحاولة بعد التحقق. |
| Offline/slow network | رسالة واضحة؛ لا Spinner أبدي ولا إعادة إرسال تلقائية لعملية كتابة. |

لا تخلط بين `loadingList` و`isCreating` و`cancellingId`. لا تعتمد على navigation أو إعادة فتح التطبيق لإعادة state إلى وضع طبيعي.

### 10.5 قواعد القوائم والبيانات

1. اعتبر نتيجة الـRPC أو الـrepository مصدر الحقيقة، لا تخمّن أن العملية نجحت من الضغط على الزر.

1. بعد إنشاء سجل، أضفه مرة واحدة فقط مع منع التكرار بالـ`id`.

1. بعد إلغاء أو حذف منطقي، أزل السجل من القائمة التشغيلية أو بدّل حالته محلياً وفق ما يعيده الخادم.

1. لا تعرض إجراءً لا تسمح به حالة السجل. مثال: لا تعرض «إلغاء Pending» إذا كان `cancelled_at` أو `activated_at` موجوداً.

1. عند تحديث قائمة من الشبكة، لا تمسح نتيجة Mutation محلية حديثة بلا سبب. اعتمد نتيجة الخادم النهائية عند وصولها.

1. عند وجود احتمال نجاح Mutation مع فقدان الرد، أعد جلب السجل أو القائمة ثم اعرض حالته الحقيقية. لا تنشئ نسخة ثانية ولا تطلب من المستخدم الضغط عشوائياً مرة أخرى.

### 10.6 الأداء ومنع الـLag

1. استخدم `FlatList` للقوائم القابلة للنمو. لا تستخدم `ScrollView` لقوائم الطلبات أو الكباتن أو السجلات الطويلة.

1. أعطِ كل عنصر `keyExtractor` ثابتاً من `id`. لا تستخدم index كمفتاح لسجلات قابلة للتغيير.

1. لا تنفذ RPC أو `setState` أو حسابات ثقيلة أثناء render. اجلب البيانات من `useEffect` أو handler واضح فقط.

1. لا تستخدم `useEffect` بلا dependencies محددة، ولا تجعل Effect يعيد تشغيل نفسه عبر state يحدّثه.

1. لا تستخدم `useMemo` و`useCallback` عشوائياً. استعملهما فقط لقائمة كبيرة أو callback يمرر إلى عنصر memoized أو حساب مكلف معروف.

1. ابحث في القوائم محلياً بعد تحميلها عندما يكون الحجم صغيراً. عند بناء بحث شبكي، استخدم debounce وامنع الاستجابات القديمة من استبدال نتائج أحدث.

1. لا تضع FlatList داخل ScrollView قابل للتمرير. حافظ على عنصر تمرير رئيسي واحد في كل شاشة.

1. نظّف subscriptions وtimers وطلبات القراءة التي لم تعد الشاشة تحتاجها عند unmount أو تغير الحساب.

### 10.7 التعامل مع التصميم

1. التصميم النهائي يأتي من Google Stitch أو مرجع معتمد. لا تخترع Dashboard أو Tabs أو رسوم نهائية من عندك.

1. قبل وصول التصميم، ابنِ واجهة وظيفية محايدة وبسيطة فقط، مع الفصل بين:
  - Component العرض.
  - state والـhandlers.
  - استدعاءات `deliverySupabase`.

1. لا تضع منطق Supabase داخل Components صغيرة قابلة لإعادة الاستخدام. مرّر data وcallbacks من الشاشة أو Hook مخصص.

1. التزم بالعربية وRTL والنصوص الواضحة. لا تغيّر قواعد الأدوار أو الأعمال لأن التصميم لا يحتوي على زر لها.

1. عند دمج تصميم جديد، غيّر العرض فقط ما لم تطلب المهمة تغيير سلوك صريح. لا تكسر Auth أو RPC أو state lifecycle أثناء التجميل.

### 10.8 بوابة التسليم والتحقق

قبل قول «تم»، نفّذ واذكر الحالات التالية لكل وظيفة تغيّر بيانات:

| حالة القبول | يجب التحقق منها |
| --- | --- |
| مدخلات صحيحة | العملية تنجح وتنعكس نتيجتها الصحيحة في الواجهة. |
| مدخلات ناقصة أو خاطئة | تُرفض محلياً من دون طلب شبكة. |
| خطأ من الخادم | رسالة مفهومة، لا crash، ولا loading عالق. |
| ضغط متكرر | لا تنشأ أو تلغى أو تنتقل سجلات مرتين. |
| سجل تغيّرت حالته | لا يظهر له زر أو إجراء لم يعد مسموحاً. |
| صلاحية غير مسموحة | لا تعتمد الواجهة وحدها؛ الـRPC/RLS يرفض العملية. |
| شبكة بطيئة أو فشل اتصال | لا Spinner أبدي ولا retry تلقائي لعملية كتابة. |

بعد تعديل شاشة أو Routing، شغّل:

```
npx tsc --noEmit
npx expo export --platform android
```

لا تخفِ الأخطاء لتنجح الأوامر. إذا تعذر اختبار شيء على الجهاز الفعلي، اذكره بوضوح مع ما تم اختباره فعلياً وما بقي للاختبار اليدوي.

### 10.9 صيغة التسليم من الوكيل

في نهاية كل مهمة، أرسل فقط:

1. الملفات المعدلة.

1. ملخص المنطق الذي تغير.

1. جدول حالات القبول التي غطاها الكود.

1. نتيجة أوامر التحقق.

1. أي اختبار يدوي باقٍ بعبارة صريحة.

ممنوع الادعاء أن ميزة «شغالة» لمجرد أن TypeScript أو Expo export نجحا. نجاح البناء يعني أن التطبيق يترجم؛ نجاح الميزة يتطلب تحققاً من السلوك والمخرجات أعلاه.